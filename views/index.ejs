<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <style>
        table {
            border-collapse: collapse;
            margin: 20px 0;
        }
        td {
            border: 1px solid #000;
            width: 50px;
            height: 50px;
            padding: 0;
            text-align: center;
            vertical-align: middle;
        }
        input[type="radio"] {
            width: 100%;
            height: 100%;
            margin: 0;
            padding: 0;
            cursor: pointer;
            display: block;
        }
    </style>
</head>
<body>
    <h1><%= title %></h1>
    <p>Joueur actuel : <%= currentPlayer %></p>
    <form action="/" method="post">
        <table>
            <% for(let row = 0; row < 3; row++) { %>
                <tr>
                    <% for(let col = 0; col < 3; col++) { %>
                        <td>
                            <% if (boardState[row][col] === '') { %>
                                <input type="radio" name="cell" value="<%= row %>-<%= col %>">
                            <% } else { %>
                                <%= boardState[row][col] %>
                            <% } %>
                        </td>
                    <% } %>
                </tr>
            <% } %>
        </table>
        <button type="submit">Confirmer</button>
    </form>
</body>
</html>